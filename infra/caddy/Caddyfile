# Lidi Studio — Caddy reverse proxy (multi-tenant ready)
# Global config + modular site imports.
# Per-site config lives in sites/*.caddy — see sites/README.md.
# Status subdomain lives in Caddyfile.status.
# Architecture context: /docs/platform-architecture-v1.md §3 + ADR-003.
{
	email lidi@lidi.studio
	servers {
		protocols h1 h2 h3
	}
}

# Status subdomain (separate file, kept for compatibility)
import Caddyfile.status

# Per-site configurations — drop a *.caddy file in sites/ to add a tenant.
# Reload Caddy after adding a file:  docker exec lidi-caddy caddy reload --config /etc/caddy/Caddyfile
import sites/*.caddy
